<h1 class="text-center mb-3">{{title}}</h1>
<h4>Members</h4>

<ul class="list-group">
	{{#each members}}
	<!-- Use the #each /each syntax to loop through the members array element passed via the render function -->
	<li class="list-group-item">{{this.name}}: {{this.email}}</li>
	{{/each}}
</ul>